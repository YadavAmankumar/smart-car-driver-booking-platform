# TODO - Booking module crash fix

- [ ] Inspect bookingRoutes.js and bookingController.js for missing/undefined exports.
- [x] Implement missing controller exports required by existing routes (without changing unrelated modules).
- [x] Align route handlers/imports to the correct exported controller functions.
- [x] Verify static routes before dynamic routes and ensure no undefined handlers.



- [ ] Start server (`npm run dev`) and confirm Booking + Payment integration still works.

